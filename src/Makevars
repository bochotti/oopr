## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
OBJECTS.models = models/meta.o \
								 models/oopr.o

OBJECTS.util	 = util/psexp.o \
								 util/symbols.o \
								 util/symlink.o \
								 util/unwind.o

OBJECTS.root	 = breakpoint.o \
								 common.o \
								 construct.o \
								 container.o \
								 enclosure.o \
								 init.o \
								 install.o \
								 reference.o \
								 source.o

OBJECTS = $(OBJECTS.models) $(OBJECTS.util) $(OBJECTS.root)

## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
CXX_STD = CXX14
PKG_CPPFLAGS = -I.
#PKG_CXXFLAGS = -fno-inline -fno-omit-frame-pointer
#STRIP =

## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
all: $(SHLIB)

## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
clean:
	rm -f $(OBJECTS) oopr.so
