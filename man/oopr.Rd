% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oopr.R, R/construct.R
\docType{class}
\name{oopr}
\alias{oopr}
\alias{ooprC-class}
\alias{ooprC}
\title{oopr}
\usage{
oopr(
  name
 ,inherits   = NULL
 ,definition
 ,parent     = parent.frame()
)
}
\arguments{
\item{name}{\code{character(1L)} \cr
The name of the class.}

\item{inherits}{\code{expression} \cr
Other classes to inherit.}

\item{definition}{\code{expression} \cr
An expression defining members.}

\item{parent}{\code{environment} \cr
The environment to assign this class to, and acts as
a parent environment for each member.}
}
\value{
\code{NULL} invisibly. An \code{ooprC} object with the slots below is assigned to
symbol \code{name} in \code{parent}.

To construct a new class instance, simply call the \code{ooprC} object as a
normal function.

\strong{DO NOT} use an assignment operator.
}
\description{
Create a class generator.
}
\details{
Each assignment inside \code{definition} will become members of the class,
see \strong{Examples}.

Specifiers can be prefixed to the name of each member, separated by \code{:}.
For more information on specifiers see \code{...}.
}
\section{Slots}{

\describe{
\item{\code{name}}{\code{character(1L)} \cr
The name of the class.}

\item{\code{inhr}}{\code{character()} \cr
Base classes, if applicable.}

\item{\code{meta}}{\code{environment} \cr
Information on all members of the class.}

\item{\code{encl}}{\code{environment} \cr
A template of what the class looks like.}
}}

\examples{
## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
#  human as a class
## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
oopr("human",,
{
## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
human <- \(first, last, age)
{
  stopifnot(
    this$isScalar("character", first)
   ,this$isScalar("character", last)
   ,this$isScalar("integer"  , age)
  );
  this$first_ <- first;
  this$last_  <- last;
  this$age_   <- age;
}
## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
public:
  ## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
  get:name <- \( )
  {
    return(sprintf(
      "\%s \%s", this$first_, this$last_
    ));
  }
  ## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
  greet <- \( )
  {
    cat(sprintf(
      "Hello, my name is \%s, aged \%i.\n"
     ,this$name, this$age_
    ));
    return(invisible(this));
  }
## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
private:
  first_ <- character(1L);
  last_  <- character(1L);
  age_   <- integer(0L);
  ## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
  isScalar <- \(type, x)
  {
    return(
         length(x) == 1L
      && typeof(x) == type
    );
  }
## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##
}) ## human
## ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ ##

john <- human("john", "smith", 50L);
print(john);
john$greet();
}
